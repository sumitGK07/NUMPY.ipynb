NUMPY in Python 🐍

This assignment focuses on understanding **Exception Handling** in Python, which helps make programs more reliable and user-friendly by handling runtime errors gracefully instead of crashing. 🛡️

In this assignment, I implemented various programs using Python's exception handling mechanism to manage common errors such as invalid user input and division by zero. I also learned how to use `try`, `except`, `else`, `finally`, and `raise` statements to build robust applications. 🚀

### 📚 Topics Covered

* ✅ Basic `try` and `except` blocks
* ⚠️ Handling `ValueError`
* ➗ Handling `ZeroDivisionError`
* 🔀 Multiple `except` blocks
* ✔️ Using the `else` block
* 🔄 Using the `finally` block
* 🚨 Raising custom exceptions with `raise`
* 🛠️ Handling multiple exceptions in a single `except` block
* 🐞 Debugging exception handling code
* 🧮 Mini Project – Calculator with Exception Handling

### 💡 Learning Outcomes

By completing this assignment, I learned to:

* Write programs that handle runtime errors effectively.
* Prevent applications from crashing due to unexpected input.
* Improve program reliability using structured exception handling.
* Use `raise` to generate custom exceptions.
* Execute cleanup code with the `finally` block.
* Build menu-driven applications with proper error handling.

### 🛠️ Technologies Used

* 🐍 Python 3
* 💻 Visual Studio Code

This assignment strengthened my understanding of Python's exception handling features and improved my ability to write safe, reliable, and user-friendly programs. These concepts are essential for developing real-world software applications. 🚀✨

